<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Setting domain project's target to netstandard2.1 so that it can be used with any other project without the framework version conflict-->
    <TargetFramework>netstandard2.1</TargetFramework>
  </PropertyGroup>

</Project>
